includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    paths:
        - app
        - config
        - database
        - routes
    level: 5
    # Filament actions stop by throwing Halt
    earlyTerminatingMethodCalls:
        Filament\Actions\Action:
            - halt
